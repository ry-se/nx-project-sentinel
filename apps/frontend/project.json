{
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f docker/frontend.DockerFile ."
      }
    },
    "targets": {
      "build": {
        "cache": true
      }
    }
  }
}
